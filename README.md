# .dotfiles
========
## Installation

```bash
$ mkdir ~/.dotfiles
$ cd ~/.dotfiles
$ git clone https://github.com/kiyodori/dotfiles.git
$ git submodule init
$ git submodule update
$ chmod +x bootstrap.sh
$ source bootstrap.sh
```
